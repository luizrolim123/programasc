#include <stdio.h>
#include <stdlib.h>
#include <math.h>

void imprime; 

int main () {

typedef struct	
struct st *valor = malloc(sizeof(struct st));
struct nota {
	float *valor ;

typedef struct valores {
   int M[][] ;
   int V[];

}
valores.v[i];
valores.m[i][j];

}





return(0);
}
